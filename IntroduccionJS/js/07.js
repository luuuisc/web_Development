// Objeto Math
let resultado;

resultado = Math.PI;
resultado = Math.round(2.6); // Redondea
resultado = Math.ceil(2.9); // Redondea hacia arriba 
resultado = Math.floor(2.9); // Redondea hacia abajo
resultado = Math.sqrt(144); // Raiz cuadrada
resultado = Math.abs(-500); // Valor absoluto convierte negativo a positivo
resultado = Math.min(3,5,1,12,4,8); // Toma el minimo de un listado
resultado = Math.max(3,5,1,12,4,8); // Toma el maximo de un listado
resultado = Math.random(); // Numero aleatorio entre 0 y 1
resultado = Math.floor(Math.random() * 30); // Numero aleatorio entre 0 y 30


console.log(resultado);